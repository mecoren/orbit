# 同步代码全面审查与系统性优化报告（2026-09-14）

- **日期**：2026-09-14
- **性质**：全面审查（只读探查 → 修复落地 → 全链回归）——继 09-07 全量排查（P0 十项）与 09-13 专项探查（S1-S35）后的第三轮，聚焦：遗留项核实、新问题挖掘、可安全修复项落地
- **范围**：cloud_sync 全模块（engine/push/pull/merge/attachments/db_loader/state/meta/fingerprint/crypto_io/paths/progress/modules/error）、sync/sync_adapters（适配器工厂/S3/WebDAV/HTTP/错误分类/sync_bundle）、sync_crypto（service/bundle_io/meta_store）、sync_registry、api 层（cloud_sync_api/full_sync_backup_api/asset_api）、调度器（sync_scheduler/on-change watcher）、attachment_repo
- **基线**：docs/同步功能专项探查报告-2026-09-13.md（批 A/B/C 已落地 S1-S13 中的可修项）；本报告核实其遗留项现状并继续收口

---

## 一、总体结论

1. **同步架构核心正确性已在前两轮收敛**：LWW+version 平局裁决确定性收敛、墓碑时间戳裁决、复活裁决防重复行、白名单双向校验（读侧 db_loader + 写侧 merge）、push 先 meta 后 data 顺序、原子落盘、空数据覆盖守卫、KeyMismatch 守卫矩阵——全部在位且有测试。
2. **本轮发现的最大缺陷是「S7 空列表防御反向锁死首台设备附件上传」**：09-13 批 B 加入的防御未区分「列表探测异常」与「云端真的没有附件」两种状态，首台设备从零同步时每轮被误判为前者，附件永久无法上传（其他设备永远拉不到）。这是**前次修复引入的回归**——防御性代码必须区分"异常态"与"初始态"，本次以存在性探测三分叉修正。
3. **S31 的 GC↔pull 对打架循环确认成立且已修复**：pull 差集只看「云端有-本地无」不看本地引用，GC 删除无引用附件后云端孤儿每轮被拉回、插占位行、再被 GC 删，无限循环。修复为差集增补活跃引用过滤，孤儿留云端不下载。
4. **性能**：push 空库守卫原先每轮做全表加载→len()→再 COUNT 复核→第三次加载，万行级模块每轮多两次全量扫描+一次全量物化；改 COUNT 后非空库仅索引扫描即过。
5. **遗留但评估为「合理推迟」的项**（S4 multipart/S15 流式/S16 墓碑 GC/S14 push_only 先 pull/S18 行级 LWW 字段盲覆盖/S25 v2 盐强度/S26 密码 zeroize/S29 dir_cache TTL）：维持 09-13 的推迟决策，理由复核后依然成立（触发规模未到/需产品拍板/跨端密钥链路联动单独立项），详见 §五。

---

## 二、本轮修复清单（6 commit，全链回归通过）

| # | commit | 定级 | 问题 | 修复 |
|---|---|---|---|---|
| F1 | `beaacd7` | **P0（功能硬故障）** | 附件 push 的 S7 空列表防御把首台设备首传场景永久死锁：云端 assets/ 目录不存在 → list 404 → 空列表 → 每轮判"疑似探测异常"跳过上传 | 空列表时抽本地首个未上传附件做 `asset_exists` 探测三分叉：云端真实存在→防御拦截（原 S7 场景）；404 确认不存在→真空云放行（首传/清空后重传）；探测出错→保守跳过待下轮 |
| F2 | `9f44ed8` | P2（性能） | push 空库守卫（P0-5）先 `load_module_items` 全量加载取 len()，触发时再逐表 COUNT 复核，`push_single_module` 第三次加载同一模块——每轮 push 三次加载 | 守卫判空直接走逐表 COUNT（与 local_items_look_empty 同口径），去掉前置全量加载；语义等价（都统计 is_deleted=0） |
| F3 | `68ea475` | **P1（中断一致性）** | rekey 顺序：`mark_all_unuploaded` 在 push_all 之后——push 中断（硬失败路径）时附件 is_uploaded 仍为 1，重试 rekey 不重传附件，云端留下「新 Key 模块 + 旧 Key 附件」混合态（S32） | 清零提前到任何云端写入之前；中断多少次，重试必然重新收集全部本地缓存附件，收敛到一致状态 |
| F4 | `7a6eff0` | P1（漏拉窗口） | pull 跳过判定仅比 remote_fp——push 中断产生「新 data + 旧 _meta」时 _meta.fp 是旧值，本端 remote_fp 与之相等即永久跳过，新 data 漏拉（S17） | `should_skip_pull` 提取纯函数：fp 一致且非空时，若 `_meta.updated_at` 晚于本地 `pulled_at`（远端发生过本端未见写入）强制走下载分支；`updated_at=0`（旧格式）保持原跳过语义 |
| F5 | `26af546` | **P1（循环+数据质量）** | S31 打架循环：GC 删无引用附件 → pull 朴素差集把云端孤儿拉回 → 插占位行（且 size_bytes 恒 0）→ 下轮 GC 又删 → 无限循环耗磁盘流量；占位行 size=0 使 UI 展示与上限校验失真 | pull 差集增补「活跃引用过滤」（`get_active_referenced_hashes`：todo_task_attachments is_deleted=0 的 DISTINCT hash）；孤儿留云端不下载、不插行；`ensure_local_cached` 占位行记录真实 size_bytes；吞错收敛进 errors（S9 口径）。新引用不漏：关联表随 todos 模块先于附件 pull 合并（sync_now/pull_then_push 阶段顺序保证） |
| F6 | `c162254` | P2（可维护性） | 同步域死代码与陈旧文档：push.rs/pull.rs 的 `upload_attachment`/`download_attachment` 零调用方死 pub 函数；traits.rs 与 full_sync_backup_api.rs 文档引用不存在的 `RemoteFile.path` 字段（S33）；mod.rs 头注释「14 个业务大类」与实际单模块不符、附件路径 `media/` 与实际 `assets/` 不符 | 删除死函数及 mod.rs 导出；修正文档指向实际字段 name 与调用方拼装口径；mod.rs 头注释对齐现状 |

**每项修复的定向测试**：
- F1：`empty_cloud_list_allows_first_upload_after_probe_miss`（首传放行）/ `empty_cloud_list_blocks_when_probe_finds_existing_asset`（探测命中拦截）/ `empty_cloud_list_blocks_on_probe_error`（探测失败保守）/ `cloud_listed_hash_is_marked_without_reupload`（既有差集回归）
- F2：push 既有 11 项测试回归（s8 集成用例覆盖守卫路径）
- F3：engine 37 项测试回归（rekey 未解锁拒绝、混合态硬失败路径）
- F4：`should_skip_pull` 真值表 4 项（常规跳过/强制拉取/零值兼容/fp 失配）
- F5：`pull_downloads_referenced_attachment_with_real_size`（拉回+真实 size）/ `pull_skips_orphan_attachment_without_reference`（孤儿不落盘不插行）/ `pull_downloads_attachment_whose_link_arrived_same_round`（同轮新关联不漏拉）
- F6：全量编译 + 502 项 core 测试回归

---

## 三、审查确认在位的关键正确性机制（第三轮复核）

| 机制 | 状态 | 证据 |
|---|---|---|
| LWW 平局裁决（updated_at + version 次级键）确定性收敛 | ✅ | merge.rs `decide_lww` + 6 项单测 |
| 墓碑时间戳裁决（deleted_at vs updated_at，保留原始删除时间） | ✅ | merge.rs `apply_tombstones` + Fix-02 用例 |
| 复活裁决（本地墓碑 + 远端较新存活 → UPDATE 不 INSERT） | ✅ | merge.rs Fix-01 分支 + 2 项用例 |
| 表白名单双向校验（读侧 db_loader / 写侧 merge `_table` 路由） | ✅ | P0-9 用例 |
| push 先 meta 后 data（中断窗口多删不漏删） | ✅ | push.rs P0-7 注释与顺序 |
| 空数据覆盖守卫（删库重装防静默清空云端） | ✅ | push.rs P0-5 + `local_items_look_empty` 复核 |
| P0-6 失败模块跳过 push（防陈旧覆盖） | ✅ | pull `failed_modules` → push `skip_modules` 链路 |
| KeyMismatch 守卫矩阵（cloud_has_module_data fail-closed + decide_auto_upload_behavior） | ✅ | engine.rs 真值表 4 项用例 |
| Data Key 解密探针（进 pull 流水前快速失败） | ✅ | `probe_data_key_with_global_meta` + v2 跨密码用例 |
| v2 确定性密钥（同密码跨设备同 Key，KeyMismatch 分叉态结构性消灭） | ✅ | `derive_data_key_v2` + 跨实例用例 |
| 限流/认证错误类型化（RateLimited 长退避 30/60/120s；Auth 首错即返） | ✅ | S6 批 + 10 项用例 |
| 调度器账本持久化（重启不全量同步；失败保留快速重试窗口） | ✅ | sync_scheduler.rs S13 |
| is_running try_lock 探测（忙则跳过不挂起） | ✅ | engine.rs S5 + 2 项用例 |
| push 模块间错误隔离（单模块失败不中断 state 应用/_meta 上传）+ rekey 例外硬失败 | ✅ | push.rs S8 + engine.rs 混合态防护 |
| 附件原子落盘 + sha256 内容校验 + Windows rename 兜底 | ✅ | attachments.rs P0-8 |
| 附件路径三口径统一（assets/{hash}.waitsync + 裸 hash 回退） | ✅ | S1 批 + BasePathAdapter 四方法对齐 |
| S3 分页（>1000 对象）+ SigV4 合规 + OSS service 推断 | ✅ | 09-13 §五矩阵复核 |
| WebDAV resourcetype 目录识别 + PROPFIND 统一错误框架 | ✅ | 同上 |
| 附件 GC「云端不删」多设备安全口径 | ✅ | asset_api `gc_local_attachments` 注释 |

---

## 四、确认存在但本轮不修的项（含 09-13 遗留评估复核）

| 项 | 定级 | 现状 | 不修理由（复核后维持） |
|---|---|---|---|
| S4：50MB 附件单 PUT + 30s 总超时 + 无 multipart/Range | P1 | 在位（timeout 已按批 B 改 read 语义缓解） | 架构级迭代（multipart 分片协议 + 断点状态机），触发条件为用户实际使用 50MB 级附件；先记录，达到规模再立项 |
| S15：整模块 fetch_all + 全量 JSON + 整包加密三重内存峰值 | P1 | 在位 | 同步规模达到万行级前收益不触发；届时按模块分片设计（与 S16 联动） |
| S16：墓碑集无上限无 GC | P2 | 在位 | 同上（墓碑 GC 需与全量备份/多设备保留窗口联动设计） |
| S14：push_only 的 reconcile 无合并本地覆盖 | P1 | 在位 | 与 on-change 5s 防抖的延迟预算冲突，需产品拍板；当前有对端 pull LWW 抢回兜底 |
| S18：行级 LWW UPDATE 字段级盲覆盖 + conflict_count 恒 0 | P1 | 在位 | 行级 LWW 固有语义边界；字段级合并是 CRDT 级演进，单独立项 |
| S25：v2 确定性派生盐仅 1 次 PBKDF2 迭代 | P2 | 在位 | 代码内已有取舍注释：单次 PBKDF2 仅派生盐本身，真正强度在 data_key 的 600k 轮；升级需跨端密钥链路联动（全部设备 meta 版本协商），单独立项 |
| S26：sync_password 明文常驻无 zeroize | P2 | 在位 | zeroize 需密钥材料全链路（String→Zeroizing 重构）波及桥层；本地优先威胁模型下优先级低于云同步正确性 |
| S29：WebDAV dir_cache 无 TTL | P2 | 在位 | 外部删除云端目录是罕见运维场景，重启即恢复；加 TTL 需权衡限流（每 TTL 一次 PROPFIND） |
| 每次同步新建 adapter（S23） | P3 | 在位 | reqwest Client 池是进程级的，连接复用实际已发生；adapter 对象本身构造轻量 |
| S35：infer_region 死代码 | P3 | 在位 | 无调用方（region 由配置显式传入且校验非空）；保留作参考实现，已纳入死代码清单候选 |

**结论**：以上均为「触发规模未到 / 需产品决策 / 需跨端联动」的架构级演进项，本轮修复不引入任何与其冲突的代码。

---

## 五、全链回归验证矩阵（2026-09-14）

| 检查 | 结果 | 备注 |
|---|---|---|
| `cargo test --workspace` | **505 passed / 1 failed** | 唯一失败 `two_instance_convergence_over_webdav`：本机无 WebDAV 服务（127.0.0.1:8123 探针不可达），AGENTS.md 记载的已知环境依赖失败，非回归 |
| `cargo clippy -p orbit-core --all-targets` | 22 warnings | 基线持平口径（09-13 记 20，+2 为本批新测试的 assert_eq bool 风格；engine.rs:60 collapsible if / push.rs:77 too_many_arguments 为基线存量，经 stash 对比确认非本批新增） |
| `cargo fmt --all --check` | 通过 | |
| `cargo check`（桌面壳嵌套 workspace） | 通过 | |
| `pnpm typecheck` | 通过 | |
| `pnpm vitest run`（apps/desktop） | **252 passed** | |
| `pnpm e2e`（Playwright） | **18 passed** | |
| `flutter analyze` | No issues | |
| `flutter test` | **274 passed** | |

---

## 六、实施步骤回放（供后续同步域改动参考）

1. **只读探查**：通读 sync/sync_adapters/sync_crypto/cloud_sync/sync_registry/调度器/api 层全部源文件 + 09-07/09-13 两份既有报告，建立「已修/遗留/新问题」三张清单
2. **遗留项逐项核实**：对 09-13 的 S14-S35 逐项回源码验证现状（在位/已修/已恶化），确认推迟理由仍成立
3. **新问题挖掘**：重点复查前次修复的副作用（发现 F1 即 S7 防御的回归）、跨模块交互（GC↔pull、merge↔附件差集、rekey 编排顺序）、中断窗口推演（F3/F4）
4. **修复落地**：单功能单 commit，每项带定向回归测试；优先级 = 数据安全 > 功能可用 > 性能 > 可维护性
5. **全链验证**：core 测试 + clippy 基线对比（stash 法）+ 双端四套件
6. **文档回写**：本报告 + CHANGELOG Unreleased 补条目

---

## 七、后续建议（按优先级）

1. **真机双设备同步验收**（F1/F5 涉及附件链路，建议 A/B 两台设备实测附件首传与孤儿清理）
2. S4 multipart 附件分片：当用户反馈出现 20MB+ 附件同步失败时立项
3. S16 墓碑 GC + S15 模块分片：同步数据达到万行级（当前 push 体积主要来自墓碑全量）时一并设计
4. conflict_count 落地：merge 已有 skipped/updated 统计，补冲突记录暴露到 sync_history（S28 半项）
