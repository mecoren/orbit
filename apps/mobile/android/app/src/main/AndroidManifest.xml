<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 网络：云同步（WebDAV/S3）、云端备份上传、（M4 验收发现：Flutter 模板
         仅在 debug/profile manifest 声明 INTERNET，release 构建不声明则
         app uid 的 socket 被内核 eBPF 拦截——云同步在真机全挂） -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Phase 7 本地通知：Android 13+ 通知运行时权限声明（运行期经
         flutter_local_notifications requestNotificationsPermission 请求，
         用户拒绝则应用内静默降级为 toast 提示） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- P2 提醒升级（后台闹钟通道，flutter_local_notifications 16+ 官方
         要求宿主 manifest 自行声明）：
         - RECEIVE_BOOT_COMPLETED + WAKE_LOCK：重启/应用更新后由插件
           ScheduledNotificationBootReceiver 恢复全部 pending 闹钟；
         - SCHEDULE_EXACT_ALARM：zonedSchedule(alarmClock) 精确闹钟
           （Android 12+；未授予时代码回落 exactAllowWhileIdle，
           官方二选一之一，USE_EXACT_ALARM 限日历/闹钟类上架审核）；
         - VIBRATE：Importance.high 渠道提醒震动。 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- 生物识别解锁（指纹闸门 local_auth）：BiometricPrompt 运行时
         权限声明（API 28+ 系统对话框；API 23-27 由插件回落
         FingerprintManager 兼容路径，不需要此权限） -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 小而美批次④ 分享接收：系统分享面板出现 循迹（纯文本） -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications：到点触发（系统闹钟唤醒直接弹通知，
             不依赖 Dart 进程）+ 重启恢复 + 推迟 action 后台派发（官方
             README §AndroidManifest.xml setup 原样声明；exported 均按
             官方示例——BootReceiver 收系统广播必须 exported=true，其余
             false） -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="true" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Android 桌面小组件（#3 存在感链条；TickTick/MS To Do 同款入口）：
             APPWIDGET_UPDATE 系统广播必须 exported=true；数据面由
             home_widget SharedPreferences 提供（Dart TodoWidgetService 写入） -->
        <receiver android:exported="true" android:name=".TodoWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info"/>
        </receiver>
        <!-- 小组件勾选广播（模板 PendingIntent 目标；exported=false 仅本应用
             RemoteViews 可触发） -->
        <receiver android:exported="false" android:name=".TodoWidgetToggleReceiver"/>
        <!-- 小组件列表数据工厂（RemoteViewsService 跨进程装配行视图） -->
        <service
            android:exported="false"
            android:name=".TodoWidgetViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"/>
        <!-- 快捷设置磁贴（#3）：副标题实时计数与小组件共用同一数据面快照 -->
        <service
            android:exported="true"
            android:name=".TodoTileService"
            android:icon="@drawable/widget_check_filled"
            android:label="@string/widget_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
