# 循迹（Orbit）隐私声明

> 适用范围：循迹（Orbit）桌面版（Windows / macOS / Linux）与 Android 版。
> 生效日期：2026-09-03 ｜ 版本：随应用发布（源码位于仓库根 `PRIVACY.md`）

## 一、总原则

循迹是一款**本地优先**的任务管理应用。你的数据默认保存在你自己的设备上；
云同步功能默认关闭，只有在你主动配置并启用后才会与你自己指定的存储
（WebDAV / S3）通信。我们不运营任何在线服务，应用内没有账号体系。

## 二、端到端加密承诺（E2E）

云同步与全量备份对数据内容全程端到端加密：

- 同步密码在你的设备上经 PBKDF2 派生密钥，数据在你的设备上完成 AES-GCM
  加密后**才**上传；你的 WebDAV / S3 服务端只存储密文，任何第三方
  （包括存储服务提供者）都无法读取明文。
- 同步密码不由本应用传输到任何服务器；桌面端可选择将其缓存在操作系统
  钥匙串中（service 名 `orbit.sync-crypto`），仅存于本机。
- 全量备份包（`.orsync`）同样以同步密码加密，无密码不可解包。

**平台边界（请知悉）**：

| 平台 | 本地数据库 | 同步配置文件 | 说明 |
|---|---|---|---|
| 桌面（Win/macOS/Linux） | SQLCipher 加密（可设主密码） | 加密存储（`sync_config.enc`） | 钥匙串派生 CEK 恒走加密分支 |
| Android | 明文 SQLite（MVP 降级） | 明文 `sync_config.json`（降级分支） | 无系统钥匙串 CEK 可用时的既定降级（ADR 0001），后续版本将攻坚 SQLCipher |

即：**云端永远是密文**（E2E 承诺不受平台降级影响）；平台差异仅在于
「本机数据目录」中配置文件与数据库的静态加密强度。介意此差异的 Android
用户请勿在本机保存含敏感字段的同步配置，或等待后续加密攻坚版本。

## 三、我们不收集什么

循迹**不内置任何**遥测、崩溃上报、使用统计或分析 SDK：

- 不上报崩溃现场：崩溃信息仅落本地 `logs/crash.log`，是否反馈由你自行决定；
- 不上报任何功能使用数据；
- 不请求与功能无关的系统权限（Android 端仅申请「通知」权限，用于本地提醒）；
- 不含广告、推广或第三方追踪组件。

## 四、数据存储位置

- 本机数据目录（桌面：`%APPDATA%/cn.wait.orbit` / `~/Library/Application
  Support/cn.wait.orbit` / `~/.local/share/cn.wait.orbit`；Android：应用
  私有目录 `cn.wait.orbit`）：数据库、同步状态、备份、日志，均不离开设备
  （除非你主动执行导出/备份/上传）。
- 云端（仅当你启用同步）：`{你配置的 base_path}/orbit/` 下的**加密**同步包
  与**加密**备份包，由你自己控制的服务器托管。
- 节假日数据服务（timor.tech 公益接口，`/api/holiday/year/{年}`）：
  日历视图展示中国法定节假日所需的数据源。默认每天固定时刻自动更新一次
  （错过则下次打开应用时补更），也可在日历视图手动更新；请求只携带
  年份（URL 路径），不带任何身份信息、账号或你本地的任务数据，返回数据
  缓存在本地数据库，离线/失败时回退内置的年度法定节假日表，功能可用
  不依赖该接口。
- 除上述同步地址与节假日数据接口外，应用不访问其他网络地址。

## 五、日志

发布构建的日志写入本机 `logs/` 目录并自动轮转保留。日志遵循脱敏红线：
不记录主密码、DB Key、同步密码、任何密钥派生值、WebDAV/S3 完整凭据，
以及数据内容；仅含表名、行数、指纹前 8 位等元信息。

## 六、权限使用（Android）

| 权限 | 用途 | 拒绝的后果 |
|---|---|---|
| 通知（POST_NOTIFICATIONS） | 到期待办提醒的本地系统通知 | 降级为应用内 toast 提示，功能不崩溃 |

不申请网络权限以外的任何敏感权限（相册、定位、通讯录等一概不用）。

## 七、明文导出

应用提供明文数据导出能力（JSON / CSV），由你在本机主动触发并选择保存路径。
导出文件为**未加密**明文，请妥善保管。

## 八、开源与审计

循迹以 MIT 许可证开源，加密与同步实现全部位于可审计的 Rust 核心库
（`crates/orbit-core`），欢迎任何人审阅与验证本声明的每一条承诺。

## 九、变更

本声明随版本更新，变更历史见仓库 `CHANGELOG.md`。重大隐私语义变更
（如新增网络访问、新增权限、加密边界调整）会在应用更新说明中显著提示。

---

联系方式：请通过仓库 issue 渠道反馈（本应用无服务器端账号体系，
故无在线账户数据可处置；卸载应用即彻底删除本机全部数据）。
