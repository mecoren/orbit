# 05 UI 复刻规格 —— 移动端（Flutter → React/Tailwind）

> 蓝本：`wait-home/mobile/lib/modules/todo/*`。技术形态为 **React 重写**（Tauri 2 WebView），目标是观感与交互逐像素对齐 Flutter 版。

## 一、信息架构决策

| 链路 | 状态 | MVP 取舍 |
|---|---|---|
| `侧栏首屏 → 任务子列表 → 详情全屏` + `底部抽屉表单` | **已接线**，是现网实际 UX | ✅ 复刻基准 |
| `todo_list_page.dart` 完整单列表版（双行玻璃标题栏+视图选择器+搜索展开） | 未接线设计稿 | ⏸ 记 M6+ 增强 |

路由表（react-router，栈式导航语义）：

```
/todo                       侧栏首屏（快捷视图+项目）
/todo/tasks?view=|projectId=|ungrouped=1    任务子列表（三参数互斥）
/todo/:id                   详情全屏
新建/编辑：底部抽屉表单（非独立页；蓝本另有 todo_form_page.dart 不采用）
```

## 二、全局视觉基座

> 移动端**不接**桌面端的 OKLCH 六色板体系（01 文档 §3.4：移动端外观页三态主题〈跟随系统/浅色/深色，默认跟随系统〉+ 字号/字重三档 + todo 强调色）；本节基础色为 Flutter 版原值平移，勿与 `color-theme.ts` 色板逻辑混用。

### 2.1 双强调色体系（两个 Context 并存，勿混用）

| Context | 默认值 | 应用位置 |
|---|---|---|
| 模块色 `ModuleAccent(todo)` | `#3B82F6` | checkbox 填充、SectionCard 标题、状态选中前景、标签边框等模块身份元素 |
| 全局主题色 `themeAccent` | `#4E8CFF`（MVP 固定默认值；自定义入口随移动设置页记 M6+） | GlassFab、LoadingIndicator(EqSpinner)、表单 SelectField accent |

### 2.2 明暗两套基础色（设计系统 v2）

> **v2 改版说明（2026-09-20）**：旧版直接平移 wait-home 色值，页面底 `#F3F3F3`
> 与卡片 `#F9F9F9` 仅差 2.4%，叠加 `elevation:0` 无阴影 + 0.3px 分割线，
> 整屏糊成一片（"灰底灰卡、没层次"根因）。v2 改为**分层白卡**：浅灰底 +
> 纯白卡片 + 柔和阴影，并补齐 `outline` 描边与三级文字。数值真源见
> `core/theme/app_colors.dart`（下表为速查镜像）。

```
light: bg #F5F6F8 · surface #FFFFFF · surfaceSecondary #F2F4F7 · outline #E4E8EF
       text #171A21 · body #303642 · sub #5F6875 · divider #ECEEF3
dark:  bg #0F1115（不用纯黑） · surface #181B21 · surfaceSecondary #21252C · outline #2E343D
       text #EDEFF3 · body #C7CDD8 · sub #98A1B0 · divider #262B33
阴影（AppElevation e1~e4，冷调 #101828）：
       e1 列表卡 0 1px 3px 4% · e2 悬浮/FAB 0 4px 6px 8% · e3 底部抽屉向上 · e4 对话框
      暗色阴影几乎不可见，层次由表面分级 + outline 描边承载
玻璃 tint（白玻璃 v2）：light rgba(255,255,255,.75) · dark rgba(24,27,33,.75)
逾期红固定 Material red500 = #F44336（非 Tailwind red）；收藏黄固定 #FACC15
priority: 0 #D1D5DB 1 #6B7280 2 #3B82F6 3 #F59E0B 4 #EF4444 5 #DC2626
status:   pending #6B7280 doing #3B82F6 done #22C55E
quickView: all #3B82F6 undone #F59E0B done #22C55E today #EF4444 week #F59E0B favorite #8B5CF6
```

### 2.3 图标体系
统一 **Material Symbols Rounded**（不用 lucide），名称随组件标注。字体加载失败时的兜底：显示同尺寸占位圆点，不破版式。

## 三、"液态玻璃"三件套（v2：白玻璃配方）

> **v2 改版（2026-09-20）**：旧配方 tint 仅 `alpha .15~.20`，玻璃近乎全透，
> 内容从标题栏下方穿过、文字浮在杂乱像素上，可读性差且显廉价。v2 采用
> **高不透明度白玻璃**：顶部接近实白、向下渐隐，既保留玻璃质感又保证清晰度。

任何玻璃表面必须同时具备：**① backdrop blur + ② 高不透明度 tint 底 + ③ 底部分隔线**，缺一即不成立。

| 使用处 | blur(σ≈CSS px 1:1) | tint | 底部分隔线 |
|---|---|---|---|
| LiquidGlassTitleBar | `blur(16px)` | surface@75%→0 垂直渐变 | 亮色极浅灰实线（`outline`）/ 暗色五段白微光 |
| GlassFab | `blur(14px)` | surface@82%（亮）/86%（暗）+ e2 阴影 | — |
| 表单日期字段盒 | `blur(10px)` | surface@50% | — |
| 卡片/弹层上限 blurStatic | ≤ `blur(45px)` | — | — |

分隔线：`height:1px` 水平渐变五段——亮色 `outline` `[0,.9,1,.9,0]`（白高光在浅底上不可见，
故 v2 亮色改用克制描边线定界）、暗色白色 `[0,.22,.32,.22,0]`。
无级渐变模糊（标题栏顶强底弱）：上层伪元素完整 blur+tint，用 `-webkit-mask-image` 垂直渐变做 alpha 遮罩；Safari/WKWebView mask 失效时降级为分段 blur + 渐变蒙版 div。**滚动 offset=0 时必须移除模糊层**（性能与观感双要求）。
性能纪律：滚动偏移存 ref 直接改 style.opacity，模糊层独立 memo 组件（对应蓝本 RepaintBoundary/ValueListenable 优化），禁止整页 setState。

## 四、逐屏规格

### 4.1 侧栏首屏 `/todo`

- 标题栏：LiquidGlassTitleBar 单行模式（showSecondRow=false），title "待办"，右侧 actions 仅搜索钮（Material Symbols `search_rounded`）→ push `/todo/tasks?view=all`。**左端云同步状态图标**（`sync_status_button.dart`，经 `LiquidGlassTitleBar.leading` 插槽：同步中转环 / 成功对勾回弹 1.8s / 失败红点常驻；移动端无 hover，点击弹底部信息面板 = 状态 + 上次同步 + 下次自动同步估算 + 失败原因 + 「立即同步」；未配置 / 未解锁 → push `/settings/sync`）。
- 主体 CustomScrollView 三段：
  1. **快捷视图**：SectionHeader("快捷视图") + 6 个 ListTile 行——leading icon size22 用 quickView 语义色、title 16/w500 onSurface、trailing `chevron_right_rounded`(onSurfaceVariant)。图标映射：全部任务 `list_alt_rounded` / 未完成 `radio_button_unchecked_rounded` / 已完成 `check_circle_outline_rounded` / 今天截止 `calendar_today_rounded` / 本周截止 `date_range_rounded` / 收藏 `star_border_rounded`。点击 → `/todo/tasks?view=<name>`。
  2. **项目**：SectionHeader("项目", trailing=`add_rounded`(22, compact)) + 可拖拽重排列表。行结构：12×12 圆角方块(hex_color||强调色) + 名称 16/w500 + 尾部[未完成计数 badge(surfaceContainerHighest 底、radius 8、padding h8/v2、字12，仅 >0 显示) + gap8 + `drag_handle_rounded`(onSurfaceVariant@50%)]. 长按 = 删除流程；拖拽代理底色 surfaceContainerHighest。排序落库逐条 `sortOrder=i+1`，本地覆盖列表先行渲染，失败 toast"排序失败"(destructive)。
  3. **未分组**：`inbox_rounded`(22, onSurfaceVariant) + "未分组" 16/w500 + 同款计数 badge + chevron。
- 新建项目对话框：AlertDialog "新建项目" + WaitInputField(label 项目名称, autofocus) + 取消 TextButton / 创建 FilledButton；创建默认 `#3B82F6`；成功 toast"已创建/项目已创建"。
- 删除保护：有未完成任务 → 确认抽屉"无法删除"/"该项目下还有 N 条未完成任务，请先清空或移走任务后再删除。"/按钮"我知道了"（单按钮）；否则确认抽屉(destructive)"删除项目"/"确定要删除项目「x」吗？该操作不可撤销。"。**确认类交互（删除/清空/恢复/断开等）一律走 `showConfirmBottomSheet` 底部抽屉，不用 AlertDialog**（口径见 AGENTS.md；AlertDialog 仅保留带 TextField 的输入表单）。
- SectionHeader 规格：padding L16/R8/T12/B4，labelMedium、onSurfaceVariant、w600。
- 列表底部预留 space48。

### 4.2 任务子列表 `/todo/tasks`

- 标准 AppBar：返回 `arrow_back_rounded` + 动态标题（项目名/"未分组"/视图名）+ 右上 `more_vert_rounded`（MVP 无动作）。
- 列表：ListView padding top8/bottom48，每项 TodoTaskTile（§4.5）。
- 空态 EmptyState(icon=`checklist_outlined`)，文案按入口：暂无任务 / 暂无未完成任务 / 暂无已完成任务 / 今天没有截止的任务 / 本周没有截止的任务 / 暂无收藏任务 / 该项目暂无任务 / 暂无未分组任务。
- FAB：AboveBottomNavFab(extraMargin=4) 包 GlassFab(**accentColor=全局 themeAccent**)，点击弹新建表单抽屉（携带 defaultProjectId）。
- 过滤语义照抄：today=自然日相等；week=today ≤ d < today+7；勾选完成写 `status=pending|done`；星标写 `is_favorite`。

### 4.3 详情全屏 `/todo/:id`

区块顺序与容器 `_SectionCard`：elevation 0、radius 12、边框 outlineVariant@30%、内边距 16；头部 Row[标题 12/w600/**accent 色**(可带副标题 bodySmall/sub) + Spacer + trailing] → 内容；区块间距 12；页面尾留白 80。

| 区块 | 规格 |
|---|---|
| 标题区 | headlineSmall；点击文字原地变 TextField（InputBorder.none+isDense，autofocus），onSubmitted 与 onTapOutside 触发保存；trim 空/未变化仅退出编辑态 |
| 基本信息 | 行序固定：优先级(P0–P5 文本+10×10 色点，**P0「无」浅灰也显点**) / 状态(待办·进行中·已完成，选 done 自动补 done_at，切走清空) / 项目(名/未分组) / 截止日期(yyyy-MM-dd，更新后重新调度提醒) / 进度(`N%`，仅>0 显示，只读)。_InfoTile：InkWell r8 + v-padding8；Row[label bodySmall/onSurfaceVariant 固定列宽 **80** → value 前 10×10 色点 + bodyMedium] → 可编辑尾部 `keyboard_arrow_right_rounded`(20/sub)。编辑一律底部弹层选择器：头 16/w600 padding16 + ListTile 带 12×12 圆点（六档全带色） + 当前值尾部 `check_rounded` |
| 描述 | trailing TextButton"编辑"；展示态 whitespace-pre-wrap bodyMedium（空显"暂无描述" sub@50%）；编辑态 = 底部抽屉（isScrollControlled、70% 屏高上限、键盘避让）：TextField maxLines null/minLines 6/maxLength 5000 + 取消 TextButton + 保存 FilledButton（trim 空写 null；内容未变只关闭） |
| 子任务 | 副标题 doneCount/total(bodySmall)；行 = 22×22 圆形 checkbox(border2，完成填充 accent+check14 白，点按 toggle) + 标题 bodyMedium(完成 lineThrough+sub@50%) + `close`(18,compact) 删除；新增 = TextField(hint 子任务标题, autofocus, onSubmitted)+`check_rounded`；收起态 TextButton.icon(`add_rounded` 20,"添加子任务")。变更后回算 percent_done |
| 标签 | Wrap(spacing8/runSpacing8)；条目 = 10×10 色点(hexColor 圆) + 字13 bodyText + 容器边框 divider@30%、radius **8**、padding h10/v4（色点+标签名，与桌面 LabelChips 同形制）；点"编辑"进多选弹层（勾选 checkbox + 12×12 圆点 ListTiles+末项新建行：标题 + 8 色板选色），勾选即建/删关联 |
| 提醒 | 行 = `notifications_outlined`(20) + yyyy-MM-dd HH:mm + `close`(18)；"添加提醒"TextButton.icon(`add_rounded`20)→picker(initial=now+1h)→创建并重调度提醒 |
| 关联任务 | 只读胶囊：padding h8/v2、bg accent@10%、radius 8、字12 accent + "任务 #id"，行距 bottom4；relations 为空不渲染 |
| 评论 | 内容 bodyMedium + 相对时间 bodySmall/sub（刚刚/N分钟前/N小时前/N天前/yyyy-MM-dd）+ `delete_outline`(18)；底部输入框(hint 输入评论...)+`send_rounded`(accent) |
| 附件 | 行 = 类型图标(`cloud_download_outlined` 待同步 / `description_outlined` 已缓存 18) + 原名(14 单行截断) + 大小(`humanFileSize` 11/sub，待同步显"待同步") + `open_in_new_rounded`(18，图片应用内全屏预览，非图片写缓存目录提示路径) + `delete_outline`(18，确认弹窗)；trailing `add`(18) 走 file_picker 上传（单任务 20 个/单文件 50MB，内容寻址幂等） |
| 历史 | 只读轨迹（`taskActivityProvider`，时间倒序；档位默认 30、满档显「仅显示最近 30 条操作」+「显示更多」展到 core clamp 上限 100，口径同桌面 ActivitySection）：行 = 完整时间 `yyyy-MM-dd HH:mm`(11/mono/sub) + describeActivity 文案(13/bodyText)；文案格式化 `logic/activity_format.dart` 镜像桌面 `shared/activity-format.ts`（三代 detail 回退）；空态"暂无操作记录"；失效口挂 BootGate 的 `todo_activity_log` 事件精确 invalidate（不走全量链——业务事件先于轨迹 INSERT 到达会读到旧行） |

### 4.4 新建/编辑表单（底部抽屉）

容器 RecordFormBottomSheet：标题"添加待办"/"编辑待办"，保存图标 `check_rounded`（保存中换 EqSpinner(20)），snap 点 **initial .65 / min .35 / max 1.0**；内容 ListView padding 16。字段顺序：

| # | 区块 | 字段 | 规格 |
|---|---|---|---|
| 1 | 必填信息 | 标题* | WaitInputField 单行，errorText"请输入标题"；区块头=3×16 强调竖条(r2)+w8+标题 w600 |
| 2 | 基本信息 | 描述 | multiline |
| 3 | 〃 | 项目 | WaitListSelectField(选项"无项目"+全部项目, accent=themeAccent) + 尾部 `add_circle_outline_rounded`(28) 快捷建项目对话框（名称+六色 28 圆默认蓝） |
| 4 | 〃 | 优先级 | 选项来自 cfg `todo_priority`（low/medium/high/urgent/immediate），加载失败回退低/中/高；旧值不在选项中追加兜底项 |
| 5 | 〃 | 状态 | SegmentedButton 三段（待办/进行中/已完成）；上方独立 label"状态"(bodySmall/sub,left4,bottom6)；选中底 **accent@15%**、前景 accent |
| 6 | 日期 | 截止日期 | WaitDatePicker 玻璃盒（blur10+surface@50%+r12+边框 outline@30%/0.5，padding h14/v12，前缀 `calendar_today_rounded` 18，清除 `close_rounded` 18） |
| 7 | 更多选项 | 折叠开关 | Row[`expand_more_rounded`/`expand_less_rounded`(22,accent)+"更多选项"] v-padding4；编辑态已有 start/hexColor 时默认展开 |
| 8 | 〃 | 开始日期 | WaitDatePicker，间距 12×3 |
| 10 | 〃 | 颜色 | Wrap(gap10) 七个 **32×32** 圆：[''透明+`block_rounded`(16) 占位，+#EF4444/#F59E0B/#22C55E/#3B82F6/#8B5CF6/#EC4899]；选中 border **3px accent** 否则 1px outlineVariant；上方 label"颜色"(bodySmall/sub) |

保存链：校验标题非空 → create/update(payload 含全部字段) → scheduleTodoReminder（跳过无到期或已完成）→ 关闭抽屉 + invalidate 列表；失败 WaitToast(destructive,"保存失败")。编辑进入异步预填，loading 显 EqSpinner 居中 48px，失败显"记录不存在或加载失败"+返回钮。

### 4.5 任务行 TodoTaskTile

flex 行：24px 圆形 checkbox(border-2，完成填 accent+check16 白) + 标题 16/w500（完成 lineThrough+50% 透明）+ RichText 副标题行（8px 优先级色点 + 项目名 12px + `·` + 日期，逾期 #F44336）+ 星标 24px `#FACC15`。卡片底 `color-mix(in srgb, var(--surface-container-low) 50%, transparent)`、radius 12、外边距 (12,4)、内边距 (12,8)。**长按 500ms** 弹操作弹层（同时屏蔽 contextmenu）。点按 → 详情全屏。

## 五、共享组件复刻要点（React 实现）

> 标注 ⏸ 的条目属于 §一 排除的"未接线设计稿"范畴：仅作 M6+ 规格备忘，MVP 不实现、不复刻。

| Flutter 组件 | React 方案要点 |
|---|---|
| SlidingDrawerScaffold / 弹簧抽屉 | 自研或 vaul 改造：snap 点数组、touchend 按速度+阈值选点后弹簧补间（scrollSpring 参数 170/24）；拖到 min 关闭；顶部圆角 20、handle 40×4/r2/@40%；**纯平移无缩放遮罩** |
| 键盘避让 | 独立子组件订阅 visualViewport/keyboard 事件，仅给内容区加动态 bottom padding |
| ExpandableBottomSheet snap 组 | MVP 仅表单 .35/.65/1.0；筛选 .3/.55 · 排序 .3/.45 · 视图选择 .3/.6/.85 均 ⏸ M6+ |
| RecordSortDrawer | ⏸ M6+ 参考（MVP 无排序抽屉）：两列 grid；选中项只变色加粗不加背景 |
| LoadingIndicator(EqSpinner) | 移植 web 版 bounce-blocks 物理模型：重力 2600、restitution 0.62、挤压弹簧 170/14、三球延迟 0/.65/1.3s（rAF 或三 div+JS transform） |
| WaitToast / WaitAlertDialog | sonner 定制皮肤或自绘浮层，variant: default/destructive；提醒兜底 toast 整卡可点跳任务详情（onTap 不自动收） |
| WaitDatePicker | 自研日历 sheet：日视图 = 日历视图同款 `AppMonthCalendar`（medium 档：农历/节日/节气副标签 + 休/班徽标 + 周末蓝字，今天实心强调块、选中 1.5px 描边，节假日走同一份 holidayProvider 缓存）、头部相对日期副标题、日⇄年月⇄年三视图、整面板可滚（短屏不溢出）、清除/确认双按钮 r10 |
| 安全区 | `env(safe-area-inset-*)` + Android 手势条兜底 48px |

## 六、Flutter→CSS 换算速查

```
ImageFilter.blur(σ) ≈ backdrop-filter: blur(<σ>px)   // 高斯标准差语义一致，1:1
withValues(alpha:)  → rgba()/color-mix()
MediaQuery.padding  → env(safe-area-inset-*)
SilkyScrollPhysics  → MVP 接受 WebView 原生滚动（保留 overscroll-behavior；
                      Q 弹停止需接管滚动物理，成本高，记 M6+）
长按                → pointerdown 起 500ms 定时器 + contextmenu preventDefault
```

## 七、常量速查卡（复制即用）

```
// 尺寸 px
TITLE_BAR_ROW1=56 ROW2=46 NAV_BAR=56⏸ NAV_GAP=12⏸ FAB=56 ICON sm/md/lg/xl=18/22/24/28   // NAV_* 仅 M6+ 底部导航（MVP 无底部导航）
TOUCH=48 LIST_ITEM=56 RADIUS xs/small/medium/large/xl/full=6/10/14/20/28/9999
SPACE=2/4/6/8/12/16/20/24/32（+语义 pageInline/cardPadding/cardGap/sectionGap/rowInline/rowVertical）
// 行高：字阶带 height 倍数（标题 1.2~1.4 / 正文 1.45~1.5 / 标签 1.35~1.4）
// 模糊 σ
titleBar=16 fab/navBar=14 fieldBox=10 staticMax=45 scrollFade=32
// 动效
fast/normal/slow=150/220/300ms drawerSpring(1,300,25) scrollSpring(1,170,24)
// 曲线：standard=easeOutQuint 类(0.22,1,0.36,1) · emphasized=(0.2,0,0,1) 大位移转场
//       decelerate 入场 · accelerate 退场 · bounce=easeOutBack 小元素
drawerVelocity=500px/s dragActivate=14px searchToggle rotate180°/250ms expandSearch 300ms easeOutCubic
// checkbox 直径：列表行24(check16) 详情标题28(check18) 子任务22(check14)
// 色点直径：行内优先级8 InfoTile10 选择弹层12 项目行方块12 项目抽屉圆点14 新建色板28 表单色板32
// 优先级色点六档全显：P0「无」浅灰 #D1D5DB 也实心渲染（列表/日历/详情/表单/统计同源 PRIORITY 口径）
```

## 八、与桌面端有意不对称（2026-09-19 第六轮 D5）

- 桌面端写路径已乐观化（`lib/query-patch.ts` + `task-actions.ts` + `batch-actions.ts`：
  先 patch 缓存提前 paint，db-change 失效链收敛），移动端**明确不做乐观对齐**：
  Riverpod 侧是 `invalidateBusinessCaches` 统一失效模型，与 React Query 的
  按 key patch 不对构，另立项再议。

## 九、动效对齐微软 To-Do（2026-09-20 起，仅动效层）

> **只增不减**：§一 ~ §八 的布局/间距/字阶/色值/圆角/直径/层级/玻璃结构一律未动，
> 本章只补"动作过程"。所有时长与曲线取自 `core/theme/app_motion.dart`（单一来源），
> 视图内不写裸 `Duration(...)` 与裸 `Curves.*`。
>
> **2026-09-22 扩批**：§9.1 末四行（行退场 / 视图三态切换 / 骨架呼吸 / 下拉刷新）为第二批
> 新增；路由与滚动定位的硬编码数值一并收口进 `AppMotion` 别名（`pageInCubic` /
> `pageOutCubic` / `scrollSettle*`，数值零变化，`test/app_motion_test.dart` 锁值）。
> §9.2 首条"不做行移除动画"已随行退场落地作废，改为落地后的边界口径。

### 9.1 动效清单

| 对象 | 触发 | 时长 / 曲线 | 实现 |
|---|---|---|---|
| 圆形勾选框 `CircleCheckbox` | 勾选 / 取消勾选 | 底色·描边 150ms `standard`；对号缩放+淡入 150ms `bounce` | `AnimatedContainer` + `AnimatedSwitcher` 隐式动画；构造签名不变，7 处调用点零改动 |
| 完成态标题 `AnimatedStrikethrough` | 完成 / 取消完成 | 划线绘制 + 文字色过渡 260ms `standard` | 保留真实 `Text` 节点，其上叠 `CustomPaint` 按行度量绘制（支持 1 行/2 行/不限行）；控制器**初值即终态**，滚动入场不重播 |
| 列表行入场 `_RowEntrance` | 新任务出现在标准列表 | 高度展开 + 淡入 250ms `standard` | `TweenAnimationBuilder`；`play=false` 时原样返回子组件（零额外层级） |
| 拖拽抬起（任务行 / 侧栏项目行） | manual 档拖拽中 | 随拖拽进度 1.00→1.02 + elevation 0→6 | `proxyDecorator` + `Transform.scale`，抬手即销毁 |
| 底部抽屉入场 | 选择类 / 更多操作 / 表单三类 | 入场 250ms `sheetEnter`、退场 150ms | `bottomSheetMotion` 常量经 `sheetAnimationStyle` 注入 |
| 日期选择器视图切换 | 日 ⇄ 年月 ⇄ 年 | 200ms（`AppMotion.viewSwitch`） | `AnimatedSwitcher` |
| 触感反馈 | 勾选框点按、侧滑完成/删除、拖拽起止 | — | `HapticFeedback.selectionClick / mediumImpact`（与既有日历/年历调用同风格） |
| 行退场 `_RowExit` | 标准列表分支的删除 / 离场型完成 | 高度收起 + 淡出 300ms `slow` × `accelerate` | `TweenAnimationBuilder`（`_RowEntrance` 的镜像）；写库照常**立即落库**（ADR 0005），只把主列表那次失效延迟 300ms，窗内挂 `IgnorePointer` 防重复点 |
| 视图三态切换 | 列表 ⇄ 看板 ⇄ 表格 | 淡入 + 上滑 3% 200ms（`viewSwitch`；入 `decelerate` / 出 `accelerate`） | `AnimatedSwitcher`，key 只跟视图走——任务增删 / 骨架落定 / 下拉刷新不重播整列表 |
| 骨架呼吸 | 首屏初次加载（详情 / 统计 / 主列表 / 侧栏四处） | 明暗往返 1100ms（`skeletonPulse` × `breathe`） | 自绘 `OrbitSkeleton`（色取 `surfaceSecondary`）；**有旧值可守时不出现**，二级页与按钮内 loading 保留 spinner |
| 下拉刷新指示 | 主列表 / 侧栏 / 统计 / 回收站下拉 | 平台 `RefreshIndicator` 默认动效 | 看板（横滑）与表格（定表头横滚）**有意不接**——与横向拖拽抢同一手势；`edgeOffset` 下移到页头之下，否则指示条被页头遮住 |

### 9.2 有意边界与取舍（未做项 + 已落地项的范围约束，非遗漏）

- **行退场只覆盖标准列表分支**（2026-09-22 落地，原"不做行移除动画"的判断已推翻）：
  manual 重排档 / Logbook 分组档 / 看板 / 表格 / 多选态一律即时刷新（行组件与手势体系不同），
  只有标准列表这一支播退场。**延迟的只是主列表那次失效**（300ms，与退场等长），写库仍照常
  立即落库（ADR 0005 不延迟提交），其余缓存（详情 / 统计 / 回收站 / 搜索 / 标签）即时失效。
  可见面（行本体 + 逾期分组头）同帧保持旧值，不会出现"计数先减、行还在"；侧栏快捷视图计数
  与主列表同源 `todoTasksProvider`，因此也随行一起晚 300ms——代价仅"窗内返回侧栏闪一帧旧
  计数"，换来不把 ghost 状态扩散进聚合层。
- **行入场只覆盖标准列表分支**：manual 重排档（`ReorderableListView`）与 Logbook 分组档
  （done 视图）共用同一 `buildTile`，逐行追加入场需镜像数据源，回归面过大——二者维持即时切换。
- **不做星标弹跳**：星标在本应用只经长按菜单切换（无原地点按入口），而列表条件渲染 +
  虚拟化复用无法区分"刚收藏"与"滚动入场"，"出现即弹跳"会在滚动中反复触发。
- **不做滑到底直接完成**与**全完成庆祝**：前者是手势语义变更（保留 `flutter_slidable` 面板式动作），
  后者属结构级新增。
- **抽屉入场不用过冲曲线**：全高抽屉过冲会在上移后露出屏幕底部空白，观感是错位而非弹跳。

## 十、可访问性与触达热区（2026-09-22）

- **操作按钮一律带读屏标签**：走 `IconButton.tooltip`（页头菜单/返回、信息行与表单日期行清除、
  搜索清除、侧栏设置、详情子任务 / 提醒 / 评论 / 附件 / 标签、表单保存）；行内色点用 Material
  `Tooltip` 包裹（标签行色点 = "更换颜色"）。**shadcn 弹层内容里改用 `sh.Tooltip`**——弹层挂在
  Navigator Overlay 之外，Material Tooltip 找不到 Overlay 祖先会抛断言（AGENTS.md"shadcn 弹层
  三坑"第 ③ 条）。
- **触达热区 48（`AppDimens.touchTarget`），视觉直径不变**：色点用外层 `Padding` 补白，视觉尺寸
  仍取原值——标签行色点 16（Padding 16）、标签色板抽屉 40（Padding 4）、新建标签对话框色板 36
  （Padding 6）、侧栏编辑项目对话框色板 32（Padding 8）、详情标签编辑弹层色板 26（Padding 11）。
  补白只加不缩，色板 `Wrap` 间距不动（视觉间隙因此略增，属有意）。
- **回归**：`test/a11y_test.dart` 6 例——读屏标签存在性 + 色点"热区 48 且视觉直径锁定"。热区层用
  `Element.findAncestorWidgetOfExactType<Padding>()`（最近祖先）取，**不要用 `find.ancestor(...).first`**
  （它返回整条祖先链，命中哪一层取决于遍历顺序，曾量到外层 640×56 的整行 Padding）；侧栏用例收尾
  需显式推掉 `MockOrbitBridge` 的人为延迟计时器（`tester.pump` 先走时钟再画帧，帧内新建的延迟
  定时器不会被本次 pump 跑掉）。

